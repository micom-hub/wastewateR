% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r0600_wval_regioncalc.R
\name{r0600_wval_regioncalc}
\alias{r0600_wval_regioncalc}
\title{Calculating wVal for a User Defined Region}
\usage{
r0600_wval_regioncalc(
  wastewater_data_in,
  site_to_region_crosswalk = NA,
  method = "median",
  org
)
}
\arguments{
\item{wastewater_data_in}{A dataframe of wastewater data; must at least have columns of "id", "year", "week", "average_wval_calc", also must have "Geography" if site_to_regional_crosswalk is not used; most likely is output of r0100 to r0500}

\item{site_to_region_crosswalk}{Defaults to NA. If provided, should be a dataframe of "id", "Geography", "weight"}

\item{method}{Character string, either "median" or "mean", defaults to 'median'}

\item{org}{A character string, either "SC2_v1", "FLU_v1", "RSV_v1", "SC2_v2", "FLU_v2", "RSV_v2" indicating what pathogen the wastewater data represents, and the CDC methodology version of level determination the user would like to use}
}
\value{
A data frame of weekly wval levels per geography
}
\description{
Function that takes in wastewater dataframe with columns of: "id", "year",
"week", "average_wval_calc", also must have "Geography" if site_to_regional_crosswalk
is not used. (generated as output from r0100 to r0500)
}
\details{
Function also takes in site_to_region_crosswalk, method, and org.

If site_to_regional_crosswalk is provided, it must be a dataframe with columns of:
\itemize{
\item id: Site identifier or name
\item Geography: Region(s) the user is attempting to roll-up multiple sites to.
\item weight: Weight to apply to the \code{id} within the region wval calculation. Only relevant if using "mean" as \code{method}. If the user wants standard mean calculation applied, all \code{weight} should be 1.
}

If site_to_regional_crosswalk is provided, it is merged onto the wastewater dataframe
by the \code{id} variable. Only instances where the \code{id} is in both dataframes will continue
through the remainder of the calculations.

The resulting dataframe is grouped by \code{Geography}, \code{week}, and \code{year} into:
\itemize{
\item Geography: Region identifier or name
\item year: year of the sample period
\item week: week of the sample period
\item avg_min: minimum sample date within the week sample period
\item avg_max: maximum sample date within the week sample period
\item region_wval_calc: if \code{method} is 'median', then it's the median value of \code{average_wval_calc} with any \code{NA}s left out of consideration. if \code{method} is 'mean' then it's the weighted average value of \code{average_wval_calc} with any \code{NA}s left out of consideration
\item contributing_site_count: number of sites (\code{id}s) included in the calculation for the sample period week
}

Using the region_wval_calc, a \code{wval_level} is assigned to each \code{Geography}'s week of data\tabular{llllll}{
   Function Input \tab Minimal \tab Low \tab Moderate \tab High \tab Very High \cr
   SC2_v1 \tab Up to 1.5 \tab > 1.5 and <= 3 \tab > 3 and <= 4.5 \tab > 4.5 and <= 8 \tab > 8 \cr
   FLU_v1 \tab Up to 1.6 \tab > 1.6 and <= 4.5 \tab > 4.5 and <= 12.2 \tab > 12.2 and <= 20.1 \tab > 20.1 \cr
   RSV_v1 \tab Up to 4 \tab > 4 and <= 8 \tab > 8 and <= 12 \tab > 12 and <= 20 \tab > 20 \cr
   SC2_v2 \tab Up to 2 \tab > 2 and <= 3.4 \tab > 3.4 and <= 5.3 \tab > 5.3 and <= 7.8 \tab > 7.8 \cr
   FLU_v2 \tab Up to 2.7 \tab > 2.7 and <= 6.2 \tab > 6.2 and <= 11.2 \tab > 11.2 and <= 17.6 \tab > 17.6 \cr
   RSV_v2 \tab Up to 2.5 \tab > 2.5 and <= 5.2 \tab > 5.2 and <= 8 \tab > 8 and <= 11 \tab > 11 \cr
}


Finally, any instances where the \code{region_wval_calc} is not a finite value are replaced
with \code{NA}.
}
