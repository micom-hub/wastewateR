% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r0500_wval_sitecalc.R
\name{r0500_wval_sitecalc}
\alias{r0500_wval_sitecalc}
\title{Calculating wVal for individual site(s)}
\usage{
r0500_wval_sitecalc(wastewater_data_in, org)
}
\arguments{
\item{wastewater_data_in}{A dataframe of wastewater data; must at least have columns of "log_value", "baseline", "stdev", "date", "id"; most likely is output of r0100 to r0400}

\item{org}{A character string, either "SC2_v1", "FLU_v1", "RSV_v1", "SC2_v2", "FLU_v2", "RSV_v2" indicating what pathogen the wastewater data represents, and the CDC methodology version of level determination the user would like to use}
}
\value{
A data frame of weekly wval levels per site
}
\description{
Function that takes in wastewater dataframe with columns of:
\itemize{
\item log_value
\item baseline
\item stdev
\item date
\item id
(generated as output from r0100 to r0400)
}
}
\details{
Calculates the individual wVal level for all sites included in the input dataframe, using the
formula exp((log_value - baseline)/stdev)

A dataframe is created:
\itemize{
\item id: Site identifier or name
\item year: year of the sample period
\item week: week of the sample period
\item avg_min: minimum sample date within the week sample period
\item avg_max: maximum sample date within the week sample period
\item average_wval_calc: average wval of all samples in the sample period week for the given id
\item count_samples: number of samples included in the calculation for the sample period week
}

Using the average_wval_calc, a "level" is assigned to each site's week of data\tabular{llllll}{
   Function Input \tab Minimal \tab Low \tab Moderate \tab High \tab Very High \cr
   SC2_v1 \tab Up to 1.5 \tab > 1.5 and <= 3 \tab > 3 and <= 4.5 \tab > 4.5 and <= 8 \tab > 8 \cr
   FLU_v1 \tab Up to 1.6 \tab > 1.6 and <= 4.5 \tab > 4.5 and <= 12.2 \tab > 12.2 and <= 20.1 \tab > 20.1 \cr
   RSV_v1 \tab Up to 4 \tab > 4 and <= 8 \tab > 8 and <= 12 \tab > 12 and <= 20 \tab > 20 \cr
   SC2_v2 \tab Up to 2 \tab > 2 and <= 3.4 \tab > 3.4 and <= 5.3 \tab > 5.3 and <= 7.8 \tab > 7.8 \cr
   FLU_v2 \tab Up to 2.7 \tab > 2.7 and <= 6.2 \tab > 6.2 and <= 11.2 \tab > 11.2 and <= 17.6 \tab > 17.6 \cr
   RSV_v2 \tab Up to 2.5 \tab > 2.5 and <= 5.2 \tab > 5.2 and <= 8 \tab > 8 and <= 11 \tab > 11 \cr
}


Finally, any instances where the average_wval_calc is not a finite value are replaced
with \code{NA}.
}
