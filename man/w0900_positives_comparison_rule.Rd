% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/w0900_positives_comparison_rule.R
\name{w0900_positives_comparison_rule}
\alias{w0900_positives_comparison_rule}
\title{Target Positives Droplet High Limit}
\usage{
w0900_positives_comparison_rule(
  new_file_in,
  pos_samp_str,
  target_str,
  control_ids,
  thresh_size = 3
)
}
\arguments{
\item{new_file_in}{A dataframe of laboratory data}

\item{pos_samp_str}{A vector of character strings indicating the 'Sample' values of interest that indicate the positive control samples (ex. "POS")}

\item{target_str}{a vector of character strings indicating the 'Target' values of interest (ex. "N1")}

\item{control_ids}{a vector of character strings that indicate the control samples to exclude}

\item{thresh_size}{a numeric threshold limit (default value = 3)}
}
\value{
A dataframe just like the input data frame, with one new column (sample_pos_limit_flag9) added
}
\description{
This function is optional. It takes as input:
}
\details{
\itemize{
\item a laboratory data frame
\item a vector of character strings indicating the 'Sample' values of interest
that indicate the positive control samples (ex. "POS")
\item a vector of character strings indicating the 'Target' values of interest (ex. "N1")
\item a vector of character strings that indicate the control samples to exclude
\item a numeric threshold limit (default value = 3)
}

For the two 'Sample' and 'Target' character string vectors, the two should occur
in order. For example, if you're looking to check N1 samples and RV samples on
the same data plate:\tabular{ll}{
   Sample \tab Target \cr
   POS \tab N1 \cr
   RV \tab RV \cr
}


Then the two vectors should be:

Sample vector: c("POS", "RV")
Target vector: c("N1", "RV")

The threshold for marking sample rows is calculated as the average number of
positive droplets in all wells where the positive control indicator string is
in the 'Sample' column and the indicated target string is in the 'Target' column,
multiplied by the numeric threshold limit. Well rows that are NOT controls,
where the 'Target' contains the indicated target string(s), that have a 'Positives'
column value that is greater than this value are marked.

This is just a warning/notification function. The 'Sample', 'Target', and 'Positives'
value for each row that violates the threshold will be printed to the console.
A dataframe is returned from this function, and it will contain a new variable
called 'sample_pos_limit_flag9' that will have a value of 1 if the row violated
the threshold and a value of 0 if it did not violate the threshold OR if it
was not considered as part of the check.
}
