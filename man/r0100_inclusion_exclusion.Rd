% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r0100_inclusion_exclusion.R
\name{r0100_inclusion_exclusion}
\alias{r0100_inclusion_exclusion}
\title{Determine factors on which to include multiple wastewater sites into wVal calculation}
\usage{
r0100_inclusion_exclusion(
  data_file,
  pop_serve_num = NA,
  sample_type_list = NA,
  testing_site_type_list = NA
)
}
\arguments{
\item{data_file}{A dataframe of wastewater site, metadata, and measurement values}

\item{pop_serve_num}{A numeric input indicating the lower bound of acceptable population served values to include in the final data frame; Default value is \code{NA}}

\item{sample_type_list}{A character string vector indicating the acceptable sample types to include in the final data frame; Default value is \code{NA}}

\item{testing_site_type_list}{A character string vector indicating the acceptable site types to include in the final data frame; Default value is \code{NA}}
}
\value{
A data frame with (possibly) filtered row results
}
\description{
This function takes in a dataframe of wastewater data measurements and site metadata
and a series of numeric and character vector values.
}
\details{
If one does not want to consider a particular metadata point, the value can be set at NA. This is
the default for all values.

To use the pop_serve_num input, the dataframe must include a variable called \verb{population served}.
If \code{pop_serve_num} is provided a numeric value, the data frame is filtered to only include
sites with a metadata \verb{population served} value of greater than or equal to the provided
numeric value.

To use the sample_type_list input, the dataframe must include a variable called \code{sampletype}.
If \code{sample_type_list} is provided a vector of character strings, the data frame is filtered to only include
sites with a metadata \code{sampletype} included in the provided character vector.

#' To use the testing_site_type_list input, the dataframe must include a variable called \code{sitetype}.
If \code{testing_site_type_list} is provided a vector of character strings, the data frame is filtered to only include
sites with a metadata \code{sitetype} included in the provided character vector.

For all cases, if the input is left blank, all of the sites will be included regardless of
their value for the related dataframe variable.

Any/all of the three inputs can be used or not.
}
