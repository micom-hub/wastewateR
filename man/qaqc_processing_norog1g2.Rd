% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qaqc_processing_norog1g2.R
\name{qaqc_processing_norog1g2}
\alias{qaqc_processing_norog1g2}
\title{Wrapper Function - QAQC for Norovirus G1 & G2 results}
\usage{
qaqc_processing_norog1g2(
  file_in,
  lab_site_ids,
  targets_to_keep = c("NVG1", "NVG2"),
  control_strings = c("NEG", "POS", "NTC", "EXT"),
  pos_rows = data.frame(Samples = c("POS", "POS"), Targets = c("NVG1", "NVG2")),
  con_rows = data.frame(Samples = c("NEG", "POS", "NTC", "NEG", "POS", "NTC"), Targets =
    c("NVG1", "NVG1", "NVG1", "NVG2", "NVG2", "NVG2")),
  rules = c(2, 3, 6)
)
}
\arguments{
\item{lab_site_ids}{A character string vector for w0150_sample_naming_structure (A character string vector identifying the submitter sites)}

\item{targets_to_keep}{A vector of character strings indicating the Target values the user would like to allow (w0115)}

\item{control_strings}{A vector of character strings for w0125_expected_controls_present (A vector of character strings that indicate control wells) and w0150_sample_naming_structure (A vector of character strings contained in control sample names)}

\item{pos_rows}{A Sample-Target dataframe of character strings for w0400 and w0450}

\item{con_rows}{A Sample-Target dataframe of character strings for w0700 and w0750}

\item{rules}{A vector of numbers indicating what rules to remove violators from (w1000)}

\item{data_frame_in}{A dataframe of laboratory data.}
}
\value{
A list where the first element is the dataframe containing unmerged Sample-Target data points, and the second element is an error stop notification dataframe that is generated if hard stop notifications are not used
}
\description{
This function is meant to run a standard QAQC set-up using the functions available
in this package. For increased flexibility, manual construction of the functions
would be necessary
}
\details{
This function returns a list where the first element is the processed dataframe
containing unmerged Sample-Target data points along with additional columns
generated by the processing minor functions, and the second element is an
error stop notification dataframe that is generated if hard stop notifications
are not used.
}
