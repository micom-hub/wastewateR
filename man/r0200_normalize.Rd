% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r0200_normalize.R
\name{r0200_normalize}
\alias{r0200_normalize}
\title{Normalization Choice and Implementation}
\usage{
r0200_normalize(wastewater_data_in, normal_yesno, choose_norm)
}
\arguments{
\item{wastewater_data_in}{A dataframe of wastewater site, metadata, and measurement values}

\item{normal_yesno}{A character string, either "yes" or "no", indicating whether the user would like to normalize their data or leave it as is.}

\item{choose_norm}{A character string, either "microbial", "flow", "mix_flow_first", or "mix_microbial_first", indicating which normalization method to use.}
}
\value{
A data frame with a new column and also potentially filtered rows
}
\description{
This function takes in a dataframe of wastewater site data, as well as a (\code{normal_yesno}) character string indicating "yes" or "no"
if the user would like to apply a normalization method, and a (\code{choose_norm}) character string
indicating the method that would be used, if 'yes'. For this function to work,
the dataframe must, at a minimum, contain a column called \code{gcper100ml}. Depending on the use of the
normalization methods, \code{microbial_val} and/or \code{flow_val} must also be present.
}
\details{
\itemize{
\item \code{gcper100ml} = measurement of a given pathogen in the wastewater sample
\item \code{microbial_val} = measurement (in the same units as \code{gcper100ml} values) of the microbial normalization choice in the wastewater sample
\item \code{flow_val} = measurement of the water flow to normalize the \code{gcper100ml} value by
}

If the user inputs 'no' for \code{normal_yesno}, then a new column called \code{normalized_measurement}
is added, and it is set as the value of \code{gcper100ml}. Any rows where \code{normalized_measurement} is \code{NA}
are removed, and the dataframe is returned.

If the user inputs 'yes' for \code{normal_yesno}, then the user must also provide a function
input for \code{choose_norm}. This can be either:
\itemize{
\item "microbial": A new column called \code{normalized_measurement} is set as the value of \code{gcper100ml} / \code{microbial_val}
\item "flow": A new column called \code{normalized_measurement} is set as the value of \code{gcper100ml} / \code{flow_val}
\item "mix_flow_first": Both \code{gcper100ml} / \code{microbial_val} and \code{gcper100ml} / \code{flow_val} are calculated.
\code{normalized_measurement} is filled with the value of \code{gcper100ml} / \code{flow_val} first. Any missing values are filled with \code{gcper100ml} / \code{microbial_val}
\item "mix_microbial_first": Both \code{gcper100ml} / \code{microbial_val} and \code{gcper100ml} / \code{flow_val} are calculated.
\code{normalized_measurement} is filled with the value of \code{gcper100ml} / \code{microbial_val} first. Any missing values are filled with \code{gcper100ml} / \code{flow_val}
}

Any rows where \code{normalized_measurement} is \code{NA} are removed, and the dataframe is returned.

If the user inputs something that is not "microbial", "flow", "mix_flow_first", or
"mix_microbial_first", then the system will
\strong{STOP} after printing "Unaccepted function input; normalization method must be either 'microbial', 'flow', 'mix_flow_first', or 'mix_microbial_first', not: "
and the user input to the console.

If the user inputs something that is not 'yes' or 'no' for \code{normal_yesno}, then the system will
\strong{STOP} after printing "Unaccepted function input; normalization choice must be either 'yes' or 'no', not: "
and the user input to the console.
}
