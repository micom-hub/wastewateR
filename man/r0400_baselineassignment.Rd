% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r0400_baselineassignment.R
\name{r0400_baselineassignment}
\alias{r0400_baselineassignment}
\title{Determining Baseline Values}
\usage{
r0400_baselineassignment(wastewater_data_in, method_choice)
}
\arguments{
\item{wastewater_data_in}{A dataframe of wastewater site, metadata, and measurement values}

\item{method_choice}{A character string of either "cdc_v1" or "all_data" to determine method of baseline assignment}
}
\value{
A data frame
}
\description{
This function takes in a dataframe of wastewater sample data by site and a character
string of either "cdc_v1" or "all_data" to determine the method of determining
the "baseline" values for every site over time for the dataframe.
}
\details{
If \code{method_choice} is set as "cdc_v1" then the baseline is set according to the original CDC baseline
rules for SARS-CoV-2:
\itemize{
\item For each combination of site, data submitter, PCR target, lab methods, and
normalization method, a baseline is established. The “baseline” is the 10th percentile
of the log-transformed and normalized concentration data within a specific time frame.
\item SARS-CoV-2: For site and method combinations (as listed above) with over six
months of data, baselines are re-calculated every six calendar months (January 1st and July 1st)
using the past 12 months of data. For sites and method combinations with less than six months of data, baselines
are computed weekly until reaching six months, after which they remain unchanged
until the next January 1st or July 1st, at which time baselines are re-calculated.
}

If \code{method_choice} is set as "cdc_v2" then

If \code{method_choice} is set as "all_data" then the baseline is set as the 10th
percentile of all \code{log_values} for each site. The standard deviation of all \code{log_values} is calculated,
and the baseline minimum date and maximum date are set as the min and max available date
for all data per site. Any rorws where the baseline is \code{NA} are removed, and any
rows where the standard deviation is zero are removed. The final dataframe is returned.
}
