% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r0300_logtransform.R
\name{r0300_logtransform}
\alias{r0300_logtransform}
\title{Log Transformation of Values}
\usage{
r0300_logtransform(wastewater_data_in, outlier_removal, z_score_threshold = 4)
}
\arguments{
\item{wastewater_data_in}{A dataframe of wastewater site, metadata, and measurement values}

\item{outlier_removal}{A character string, either "yes" or "no" to indicate that outlier removal should/not occur}

\item{z_score_threshold}{A number, indicating the upper threshold of the z-scores that should be kept if outlier removal is occurring. Default value is 4}
}
\value{
A data frame with a new column and also potentially filtered rows
}
\description{
This function takes in a dataframe of wastewater measurement data, as well as
a character value of "yes" or "no" depending on whether the user would like to remove
outliers in a z-score method. This function also takes in a threshold z score value (numeric)
that has a default value set as 4.
}
\details{
The log() of the \code{normalized_measurement} is calculated in a new column called \code{log_value}.
As log(0) = -Inf, those rows are removed and the number of rows that were removed
because of this is messaged to the console.

If "no" for outlier_removal, then the remaining dataframe is returned from this function.

If "yes" for outlier_removal, then the dataframe is grouped by \code{id} (by site) and the average
and standard deviation of \code{log_value} is calculated. \code{z_score} is calculated as zero if
the overall standard deviation is \code{NA} or if the overall standard deviation is zero. Otherwise,
\code{z_score} is calculated as the absolute value of the sample \code{log_value} minus the overall mean log value,
divided by the overall standard deviation. Any rows where the calculated z_score is less than
or equal to the provided \code{z_score_threshold} are kept (so, any rows where the calculated z_score
is greater than the provided \code{z_score_threshold} are removed).

The number of rows that are removed due to z-score thresholding are messaged to the
console. Any extranneous variables created during the z-score calculations are removed
and the remaining dataframe is returned from this function.
}
