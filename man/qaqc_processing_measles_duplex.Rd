% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qaqc_processing_measles_duplex.R
\name{qaqc_processing_measles_duplex}
\alias{qaqc_processing_measles_duplex}
\title{Wrapper Function - QAQC for Measles Duplex}
\usage{
qaqc_processing_measles_duplex(
  file_in,
  lab_site_ids,
  targets_to_keep = c("MEVN3DEGEN", "MEVMR"),
  control_strings = c("NEG", "POS", "NTC"),
  expected_count = c(4, 4, 4),
  pos_rows = data.frame(Samples = c("POS", "POS"), Targets = c("MEVN3DEGEN", "MEVMR")),
  neg_rows = data.frame(Samples = c("NEG", "NEG", "NTC", "NTC"), Targets =
    c("MEVN3DEGEN", "MEVMR", "MEVN3DEGEN", "MEVMR")),
  con_rows = data.frame(Samples = rep(c("NEG", "POS", "NTC"), 2), Targets =
    c(rep("MEVN3DEGEN", 3), rep("MEVMR", 3)))
)
}
\arguments{
\item{file_in}{Dataframe of laboratory data}

\item{lab_site_ids}{A character string vector for w0150 (A character string vector identifying the submitter sites)}

\item{targets_to_keep}{A vector of character strings indicating the Target values the user would like to allow (w0115)}

\item{control_strings}{A vector of character strings for w0125 and w0150}

\item{expected_count}{A numeric vector of the expected number of wells for each control type.}

\item{pos_rows}{A Sample-Target dataframe of character strings for w0450 and w0500 (indicating controlst that should be positive)}

\item{neg_rows}{A Sample-Target dataframe of character strings for w0550 (indicating controls that should be negative)}

\item{con_rows}{A Sample-Target dataframe of character strings for w0700 (indicating all controls)}
}
\value{
A list where the first element is the dataframe containing unmerged Sample-Target data points, and the second element is an error stop notification dataframe that is generated if hard stop notifications are not used
}
\description{
This function is meant to run a standard QAQC set-up using the functions available
in this package. For increased flexibility, manual construction of the functions
would be necessary
}
\details{
This function returns a list where the first element is the processed dataframe
containing unmerged Sample-Target data points along with additional columns
generated by the processing minor functions, and the second element is an
error stop notification dataframe that is generated if hard stop notifications
are not used.

Please note that this function does not accommodate extraction controls - it assumes that the sample extraction and test of that extraction has already occurred.
}
